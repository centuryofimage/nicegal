- finally settle on a good sync algorithm that ignores unmodified folders
  - manual sync is still in settings
- linked folders (symlinks and junctions) now act like normal folders, and folders with unreadable files no longer resync every time
- clarify some blocking ui states, including image-only search with DINOv3
- skip previously failed videos
